============
Installation
============

At the command line::

    $ pip install openstack-kds

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv openstack-kds
    $ pip install openstack-kds